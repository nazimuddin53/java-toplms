spring:
  application:
    name: toplms
  datasource:
    url: jdbc:postgresql://localhost:5432/toplms_db
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true